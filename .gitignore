# Fichiers Terraform à ne pas versionner
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
*.tfplan

# Variables sensibles (contient le token GitHub !)
terraform.tfvars

# Claude Code local settings
.claude/
